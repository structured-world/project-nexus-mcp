# Docker ignore file for Project Nexus MCP
# This file specifies which files and directories should be excluded
# from the Docker build context to optimize build speed and reduce image size

# Version control
.git/
.github/
.gitignore
.gitattributes

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build artifacts (will be built inside container)
dist/
build/

# Testing
coverage/
.nyc_output/
test-tokens.json

# Environment files (should be provided at runtime)
.env*
!.env.example

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# OS generated files
Thumbs.db
ehthumbs.db

# Logs
logs/
*.log

# Documentation (not needed in container)
*.md
!README.md
docs/

# CI/CD files
.travis.yml
.circleci/
.appveyor.yml
azure-pipelines.yml

# Package manager cache (yarn.lock and package.json are needed for build)
.yarn/
.pnp.*

# Development tools
.eslintrc*
.prettierrc*
jest.config.*
# tsconfig.json is needed for build - don't exclude it

# Docker files (don't need to be in the image)
Dockerfile*
docker-compose*.yml
.dockerignore

# Temporary files
tmp/
temp/
*.tmp
*.temp

# Local development files
.mcp.json
providers/

# Security sensitive files
*.key
*.pem
*.p12
*.crt
*.cer
certs/

# Backup files
*.bak
*.backup